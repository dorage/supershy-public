import { onMount, type Component } from 'solid-js';

interface VipCheckProps {}

const VipCheck: Component<VipCheckProps> = (props) => {
  onMount(() => {});

  return <div></div>;
};

export default VipCheck;
